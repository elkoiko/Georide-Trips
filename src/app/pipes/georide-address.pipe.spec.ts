import { GeorideAddressPipe } from './georide-address.pipe';

describe('GeorideAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new GeorideAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
